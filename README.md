# React + Vite + Firebase

Lightweight React app powered by Vite for fast development and deployed with Firebase Hosting.

## Scripts

- `npm run dev` — run locally
- `npm run build` — create production build
- `firebase deploy --only hosting:book` — deploy app